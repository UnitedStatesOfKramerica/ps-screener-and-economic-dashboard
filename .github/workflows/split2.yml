name: Build split2 fixture
on:
  workflow_dispatch:
jobs:
  split2:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
      - run: pip install requests pandas numpy yfinance lxml
      - name: Probe splits with Yahoo feed
        env:
          SEC_EMAIL: ${{ secrets.SEC_EMAIL }}
        run: python build_split2_fixture.py
      - uses: actions/upload-artifact@v4
        with:
          name: split2
          path: split2.json.gz
          retention-days: 14
          if-no-files-found: error
